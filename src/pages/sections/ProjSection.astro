---
import Button from "../../components/Button.astro";
import ExternalPage from "../../components/icons/ExternalPage.astro";
import TitleSection from "../../components/TitleSection.astro";

import ximenaWeb from "../../assets/ximenaWeb.avif";
import doraWeb from "../../assets/doraWeb.avif";

import Nextjs from "../../components/icons/Nextjs.astro";
import Astro from "../../components/icons/Astro.astro";
import React from "../../components/icons/React.astro";
import Python from "../../components/icons/Python.astro";
import JavaScript from "../../components/icons/JavaScript.astro";

import { Image } from "astro:assets";

// class="oject-cover object-top w-[350px] h-[350px] rounded-2xl"

const TAGS = {
  NEXT: {
    name: "Next.js",
    class: "text-sm text-gray-600 dark:text-gray-200 bg-black px-4 py-2",
    icon: Nextjs,
  },
  ASTRO: {  
    name: "Next.js",
    class: "text-sm text-gray-600 dark:text-gray-200 bg-black px-4 py-2",
    icon: Astro,
  }, 
  REACT: {
    name: "React",
    class: "text-sm text-gray-600 dark:text-gray-200 bg-black px-4 py-2",
    icon: React,
  },
  PYTHON: {
    name: "Python",
    class: "text-sm text-gray-600 dark:text-gray-200 bg-black px-4 py-2",
    icon: Python,
  },
  JAVASCRIPT: {
    name: "JavaScript",
    class: "text-sm text-gray-600 dark:text-gray-200 bg-black px-4 py-2",
    icon: JavaScript,
  },
};

const PROJECTS = [
  {
    title: "Website personal - Ximena Troncoso Coach",
    description:
      "Ximena's web portfolio, where she shows what she does, the services she offers and how to hire her. Astro.js and TailwindCSS were used for its development. The reason for this decision was that Ximena wanted a page that was not very dynamic and very fast.",
    link: "https://ximenatroncoso.com/",
    github: "https://github.com/MarlonDevs/ximena-portfolio",
    image: ximenaWeb,
    tags: [TAGS.REACT, TAGS.JAVASCRIPT],
  },
  {
    title: "Front Developer Engineer - Dora.mcbo",
    description:
      "Main developer in the creation of the Dora virtual store, significant contribution as a developer, participation in UX/UI design, CEO and web positioning of the same.",
    link: "https://adventjs.dev",
    image: doraWeb,
    tags: [TAGS.NEXT, TAGS.JAVASCRIPT],
  },
];
---

<article id="projects">
  <TitleSection>Projects</TitleSection>
  <div class="flex flex-col gap-y-16 md:gap-y-8">
    {
      PROJECTS.map(({ image, title, description, link, github, tags }) => (
        <article class="flex flex-col space-x-0 space-y-2 group md:flex-row md:space-x-2 md:space-y-0  text-wrap gap-5">
          <div class="w-full md:w-3/2">
            <div class="relative flex flex-col items-center col-span-6 row-span-5 gap-8 transition duration-500 ease-in-out transform shadow-xl overflow-clip rounded-xl sm:rounded-xl md:group-hover:-translate-y-1 md:group-hover:shadow-2xl lg:border lg:border-gray-800 lg:hover:border-gray-700 lg:hover:bg-gray-800/50">
              <Image
                src={image}
                alt={title}
                class="object-cover object-top w-full h-56 transition duration-500 sm:h-full md:scale-110 md:group-hover:scale-105"
                loading="lazy"
              />
            </div>
          </div>
          {/* <Image
            src={image}
            alt={title}
            class="w-[350px] rounded-2xl"
            width="350"
            height="350"
          /> */}
          <div class="w-full sm:w-1/1 sm:max-w-lg grid justify-between items-center">
            <h2 class="text-2xl font-bold capitalize text-balance">{title}</h2>
            {/* <div class="flex flex-wrap gap-2 text-sm">
              <ul class="flex flex-row gap-x-2">
                {tags.map((tag) => (
                  <li>
                    <span
                      class={`flex gap-x-2 rounded-full text-xs ${tag.class} py-1 px-2 `}
                    >
                      <tag.icon class="size-4 color" />
                      {tag.name}
                    </span>
                  </li>
                ))}
              </ul>
            </div> */}
            <p class="text-base text-pretty my-4">{description}</p>

            {link && (
              <Button
                href={link}
                class="flex gap-1 text-base border px-2 py-1 rounded-md"
              >
              <ExternalPage class="size-6" />
                view
              </Button>
            )}
          </div>
        </article>
      ))
    }
  </div>
</article>

<style>
  img {
    image-resolution: from-image;
    image-resolution: 300dpi;
    image-resolution: from-image 300dpi;
    image-resolution: 300dpi snap;
  }
</style>
